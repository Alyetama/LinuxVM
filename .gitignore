# Release artifacts — these ship as GitHub release assets ONLY, never committed.
*.dmg
*.app

# Build output
build/
dist/
DerivedData/
.build/
target/          # Rust / Tauri
*.o
*.dSYM/

# Xcode user state
*.xcuserstate
*.xcuserdatad/
xcuserdata/

# macOS cruft
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Node / Tauri front-end (safe no-ops if the project isn't JS)
node_modules/

# Project-specific: downloaded ISOs used during local testing
*.iso

# Release notes are generated per-release, not tracked
release-notes.md
